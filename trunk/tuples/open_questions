Manipulators:
-------------

There is no way to reset (default) the delimiter characters?
Is that needed?
Also, do the delimiters ever get reset to default values?

(Is there an easy way to match a character of string in an input stream?)

any_holder
----------

const is missing from operatorT and from unwrap


